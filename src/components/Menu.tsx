import React, {useState} from 'react';
import { render } from '@testing-library/react';

const Menu = () => {
    render(
        <div className="menu">
            
        </div>
    );
}

export default Menu;